# Verification ledger 2026-07-29 (pointer)

The canonical, append-only verification ledger lives OUTSIDE all three git worktrees so it
never becomes commit noise:

  /Users/ctavolazzi/Code/active/STS-VERIFICATION-LEDGER.md

Do not maintain a second ledger here. Two ledgers means two versions of the truth, which is
the exact duplication this protocol exists to prevent. Read and append to the canonical file.
