// Generated by postforge. Do not edit.
//
// Every <script> block from the generated markup, in order, behind one init().
// Static imports were rewritten to awaited dynamic imports so they can run
// inside a function, which also lets three.js resolve from this app's own
// node_modules instead of a CDN.

export default async function init() {
  // ---- block 1 ----
  try {
    const THREE = await import('three');
    const { OrbitControls } = await import('three/examples/jsm/controls/OrbitControls.js');
    const LAYERS = [{"label":"markdown source","color":6583435,"note":"The chapter file. Contains \"> $$...$$\", display math nested inside a blockquote."},{"label":"marked tokenizer","color":16096779,"note":"THE UNTESTED LAYER. It has to recognise a math block inside a blockquote. No committed chapter had ever exercised this path."},{"label":"katex extension","color":3718648,"note":"Custom tokenizer, deliberately narrow so book prices like $25,000 never parse as math."},{"label":"KaTeX renderer","color":3462041,"note":"The only layer the first probe actually tested."}];
    const canvas = document.getElementById('stack');
    const readout = document.getElementById('stack-readout');
    const fig = document.getElementById('stack-fig');
    const renderer = new THREE.WebGLRenderer({ canvas, antialias: true, alpha: true });
    renderer.setPixelRatio(Math.min(devicePixelRatio, 2));
    const scene = new THREE.Scene();
    const camera = new THREE.PerspectiveCamera(40, 16/9, 0.1, 200);
    camera.position.set(6.5, 5.4, 9);
    const controls = new OrbitControls(camera, canvas);
    controls.enableDamping = true; controls.enablePan = false;
    controls.target.set(0, 1.4, 0);
    controls.minDistance = 7; controls.maxDistance = 24;
    controls.maxPolarAngle = Math.PI / 2.05;
    scene.add(new THREE.AmbientLight(0xffffff, 0.7));
    const key = new THREE.DirectionalLight(0xffffff, 1.4); key.position.set(5, 9, 6); scene.add(key);

    const slabs = LAYERS.map((l, i) => {
      const m = new THREE.Mesh(
        new THREE.BoxGeometry(4.6, 0.34, 3.2),
        new THREE.MeshStandardMaterial({ color: l.color, roughness: 0.45, metalness: 0.08, transparent: true, opacity: 1 })
      );
      m.position.y = i * 1.05;
      m.userData = { ...l, baseY: i * 1.05 };
      scene.add(m);
      return m;
    });
    let isolate = -1;
    fig.querySelectorAll('[data-layer]').forEach((btn) => {
      btn.addEventListener('click', () => {
        fig.querySelectorAll('[data-layer]').forEach(b => b.classList.remove('on'));
        btn.classList.add('on');
        isolate = Number(btn.dataset.layer);
        readout.textContent = isolate < 0 ? 'click a layer name' : LAYERS[isolate].note || LAYERS[isolate].label;
      });
    });
    const still = matchMedia('(prefers-reduced-motion: reduce)').matches;
    renderer.setAnimationLoop(() => {
      const w = canvas.clientWidth, h = canvas.clientHeight;
      if (w && h && canvas.width !== Math.round(w * renderer.getPixelRatio())) {
        renderer.setSize(w, h, false); camera.aspect = w / h; camera.updateProjectionMatrix();
      }
      slabs.forEach((s, i) => {
        const on = isolate < 0 || isolate === i;
        const ty = isolate < 0 ? s.userData.baseY : (isolate === i ? 1.05 : s.userData.baseY);
        s.position.y += (ty - s.position.y) * 0.12;
        s.material.opacity += ((on ? 1 : 0.12) - s.material.opacity) * 0.12;
      });
      if (!still) scene.rotation.y += 0.0014;
      controls.update();
      renderer.render(scene, camera);
    });

  } catch (e) { console.warn('[postforge] block 1 failed', e); }

  // ---- block 2 ----
  try {
    const THREE = await import('three');
    const { OrbitControls } = await import('three/examples/jsm/controls/OrbitControls.js');

    const RECORDS = [{"label":"introduction","flag":false,"values":{"claims":23,"broken":0,"resolvable":23}},{"label":"preface","flag":false,"values":{"claims":9,"broken":0,"resolvable":9}},{"label":"chapter0","flag":false,"values":{"claims":31,"broken":0,"resolvable":31}},{"label":"part-1","flag":false,"values":{"claims":1,"broken":0,"resolvable":1}},{"label":"chapter1","flag":false,"values":{"claims":64,"broken":0,"resolvable":64}},{"label":"chapter2","flag":false,"values":{"claims":42,"broken":0,"resolvable":42}},{"label":"chapter3","flag":false,"values":{"claims":16,"broken":0,"resolvable":16}},{"label":"chapter4","flag":false,"values":{"claims":19,"broken":0,"resolvable":19}},{"label":"chapter5","flag":false,"values":{"claims":29,"broken":0,"resolvable":29}},{"label":"part-2","flag":false,"values":{"claims":2,"broken":0,"resolvable":2}},{"label":"chapter6","flag":false,"values":{"claims":34,"broken":0,"resolvable":34}},{"label":"chapter7","flag":false,"values":{"claims":60,"broken":0,"resolvable":60}},{"label":"chapter8","flag":false,"values":{"claims":64,"broken":0,"resolvable":64}},{"label":"chapter9","flag":true,"values":{"claims":43,"broken":43,"resolvable":0}},{"label":"part-3","flag":false,"values":{"claims":1,"broken":0,"resolvable":1}},{"label":"chapter10","flag":true,"values":{"claims":32,"broken":32,"resolvable":0}},{"label":"chapter11","flag":true,"values":{"claims":39,"broken":39,"resolvable":0}},{"label":"chapter12","flag":false,"values":{"claims":40,"broken":0,"resolvable":40}},{"label":"chapter13","flag":true,"values":{"claims":37,"broken":37,"resolvable":0}},{"label":"chapter14","flag":false,"values":{"claims":62,"broken":0,"resolvable":62}},{"label":"chapter15","flag":false,"values":{"claims":29,"broken":0,"resolvable":29}},{"label":"chapter16","flag":false,"values":{"claims":25,"broken":0,"resolvable":25}},{"label":"chapter17","flag":false,"values":{"claims":24,"broken":0,"resolvable":24}},{"label":"chapter18","flag":false,"values":{"claims":29,"broken":0,"resolvable":29}},{"label":"conclusion","flag":false,"values":{"claims":18,"broken":0,"resolvable":18}},{"label":"appendix-a","flag":false,"values":{"claims":8,"broken":0,"resolvable":8}},{"label":"appendix-b","flag":false,"values":{"claims":330,"broken":0,"resolvable":330}},{"label":"appendix-c","flag":false,"values":{"claims":8,"broken":0,"resolvable":8}},{"label":"appendix-d","flag":false,"values":{"claims":101,"broken":0,"resolvable":101}},{"label":"appendix-e","flag":false,"values":{"claims":22,"broken":0,"resolvable":22}}];
    const METRICS = [{"key":"claims","label":"all claims"},{"key":"broken","label":"broken only"},{"key":"resolvable","label":"resolvable only"}];
    const canvas = document.getElementById('field');
    const readout = document.getElementById('field-readout');
    const countEl = document.getElementById('field-count');
    const fig = document.getElementById('field-fig');

    const renderer = new THREE.WebGLRenderer({ canvas, antialias: true, alpha: true });
    renderer.setPixelRatio(Math.min(devicePixelRatio, 2));
    const scene = new THREE.Scene();
    const camera = new THREE.PerspectiveCamera(42, 16/9, 0.1, 300);
    camera.position.set(19, 13, 27);
    const controls = new OrbitControls(camera, canvas);
    controls.enableDamping = true;
    controls.enablePan = false;
    controls.target.set(0, 3.4, 0);
    controls.minDistance = 14; controls.maxDistance = 60;
    controls.maxPolarAngle = Math.PI / 2.05;

    scene.add(new THREE.AmbientLight(0xffffff, 0.72));
    const key = new THREE.DirectionalLight(0xffffff, 1.35);
    key.position.set(8, 16, 10); scene.add(key);

    const COLS = 6, GAP = 2.4, MAXH = 9.5;
    const group = new THREE.Group(); scene.add(group);
    const geo = new THREE.BoxGeometry(1.5, 1, 1.5);
    let metric = METRICS[0].key;
    let filter = () => true;

    const bars = RECORDS.map((r, i) => {
      const mat = new THREE.MeshStandardMaterial({
        color: r.flag ? 0xf87171 : 0x38bdf8, roughness: 0.5, metalness: 0.05, transparent: true, opacity: 1
      });
      const m = new THREE.Mesh(geo, mat);
      m.position.set((i % COLS - (COLS-1)/2) * GAP, 0, (Math.floor(i / COLS) - (Math.ceil(RECORDS.length/COLS)-1)/2) * GAP);
      m.userData = r;
      group.add(m);
      return m;
    });

    const grid = new THREE.GridHelper(COLS * GAP + 4, 10, 0x2a3444, 0x1c2431);
    scene.add(grid);

    // Target heights are recomputed on every filter or metric change and eased
    // toward each frame, so switching metrics reads as the same objects changing
    // rather than a new chart appearing.
    function retarget() {
      const vals = RECORDS.map(r => r.values[metric] ?? 0);
      const max = Math.max(...vals, 1);
      let shown = 0;
      bars.forEach((b) => {
        const r = b.userData;
        const visible = filter(r);
        if (visible) shown++;
        const v = r.values[metric] ?? 0;
        b.userData._targetH = visible ? Math.max(0.25, (v / max) * MAXH) : 0.02;
        b.userData._targetO = visible ? 1 : 0.06;
      });
      if (countEl) countEl.textContent = shown + ' of ' + RECORDS.length + ' sections shown';
    }

    fig.querySelectorAll('[data-metric]').forEach((btn) => {
      btn.addEventListener('click', () => {
        fig.querySelectorAll('[data-metric]').forEach(b => b.classList.remove('on'));
        btn.classList.add('on'); metric = btn.dataset.metric; retarget();
      });
    });
    const FILTERS = { 'all': (r) => true, 'broken': (r) => r.flag, 'chapters': (r) => r.label.startsWith("chapter"), 'apparatus': (r) => !r.label.startsWith("chapter") };
    fig.querySelectorAll('[data-filter]').forEach((btn) => {
      btn.addEventListener('click', () => {
        fig.querySelectorAll('[data-filter]').forEach(b => b.classList.remove('on'));
        btn.classList.add('on'); filter = FILTERS[btn.dataset.filter] || (() => true); retarget();
      });
    });
    filter = FILTERS['all'] || (() => true);
    retarget();

    const ray = new THREE.Raycaster(); const ptr = new THREE.Vector2();
    canvas.addEventListener('pointermove', (e) => {
      const r = canvas.getBoundingClientRect();
      ptr.x = ((e.clientX - r.left) / r.width) * 2 - 1;
      ptr.y = -((e.clientY - r.top) / r.height) * 2 + 1;
      ray.setFromCamera(ptr, camera);
      const hit = ray.intersectObjects(bars).find(h => h.object.userData._targetO > 0.5);
      if (hit) {
        const d = hit.object.userData;
        readout.innerHTML = '<b>' + d.label + '</b> · ' +
          METRICS.map(m => m.label + ' ' + (d.values[m.key] ?? 0)).join(' · ');
      } else readout.textContent = 'drag to orbit · hover a column';
    });

    function resize() {
      const w = canvas.clientWidth, h = canvas.clientHeight;
      if (!w || !h) return;
      const need = Math.round(w * renderer.getPixelRatio());
      if (canvas.width !== need) {
        renderer.setSize(w, h, false);
        camera.aspect = w / h; camera.updateProjectionMatrix();
      }
    }
    const still = matchMedia('(prefers-reduced-motion: reduce)').matches;
    renderer.setAnimationLoop(() => {
      resize();
      for (const b of bars) {
        const t = b.userData._targetH ?? 1;
        b.scale.y += (t - b.scale.y) * 0.14;
        b.position.y = b.scale.y / 2;
        b.material.opacity += ((b.userData._targetO ?? 1) - b.material.opacity) * 0.14;
      }
      if (!still) group.rotation.y += 0.0012;
      controls.update();
      renderer.render(scene, camera);
    });

  } catch (e) { console.warn('[postforge] block 2 failed', e); }

  // ---- block 3 ----
  try {

    // Count up the stat tiles when they scroll into view.
    function countUp(el) {
      const to = Number(el.dataset.countTo);
      const prefix = el.textContent.replace(/[0-9,]/g, '');
      if (matchMedia('(prefers-reduced-motion: reduce)').matches) {
        el.textContent = prefix + to.toLocaleString(); return;
      }
      const start = performance.now(), dur = 900;
      const tick = (now) => {
        const p = Math.min(1, (now - start) / dur);
        el.textContent = prefix + Math.round(to * (1 - Math.pow(1 - p, 3))).toLocaleString();
        if (p < 1) requestAnimationFrame(tick);
      };
      requestAnimationFrame(tick);
    }
    document.querySelectorAll('[data-count-to]').forEach((el) => {
      new IntersectionObserver((es, o) => {
        for (const e of es) if (e.isIntersecting) { countUp(e.target); o.disconnect(); }
      }, { threshold: 0.6 }).observe(el);
    });

    // Reveal on scroll.
    const revealIO = new IntersectionObserver((es) => {
      for (const e of es) if (e.isIntersecting) { e.target.classList.add('in'); revealIO.unobserve(e.target); }
    }, { threshold: 0.12 });
    document.querySelectorAll('.reveal').forEach((el) => revealIO.observe(el));

    // Reading progress.
    const fill = document.getElementById('pf-progress-fill');
    if (fill) {
      const onScroll = () => {
        const h = document.documentElement;
        const p = h.scrollTop / Math.max(1, h.scrollHeight - h.clientHeight);
        fill.style.transform = 'scaleX(' + Math.min(1, Math.max(0, p)) + ')';
      };
      addEventListener('scroll', onScroll, { passive: true }); onScroll();
    }

    // Table of contents scroll spy.
    const tocLinks = [...document.querySelectorAll('[data-toc]')];
    if (tocLinks.length) {
      const targets = tocLinks.map((a) => document.getElementById(a.dataset.toc)).filter(Boolean);
      const spy = new IntersectionObserver((es) => {
        for (const e of es) {
          if (!e.isIntersecting) continue;
          tocLinks.forEach((a) => a.classList.toggle('on', a.dataset.toc === e.target.id));
        }
      }, { rootMargin: '-15% 0px -70% 0px' });
      targets.forEach((t) => spy.observe(t));
    }

    // Table filters.
    document.querySelectorAll('[data-filters]').forEach((input) => {
      const table = document.getElementById(input.dataset.filters);
      const count = document.getElementById(input.dataset.filters + '-count');
      const rows = [...table.querySelectorAll('tbody tr')];
      const apply = () => {
        const q = input.value.trim().toLowerCase();
        let shown = 0;
        rows.forEach((r) => {
          const hit = !q || r.dataset.search.includes(q);
          r.hidden = !hit; if (hit) shown++;
        });
        if (count) count.textContent = shown + ' of ' + rows.length;
      };
      input.addEventListener('input', apply); apply();
    });

    // Before/after slider.
    document.querySelectorAll('.pf-compare').forEach((fig) => {
      const range = fig.querySelector('.pf-compare-range');
      const before = fig.querySelector('.pf-compare-before');
      const sync = () => { before.style.width = range.value + '%'; };
      range.addEventListener('input', sync); sync();
    });

    // Sidenotes.
    document.querySelectorAll('.pf-fn-mark').forEach((btn) => {
      btn.addEventListener('click', () => {
        const open = btn.getAttribute('aria-expanded') === 'true';
        btn.setAttribute('aria-expanded', String(!open));
        btn.parentElement.classList.toggle('open', !open);
      });
    });

    // SMIL ignores CSS, so reduced motion is honoured by jumping each animated SVG
    // to the end of its timeline. Every animation uses fill="freeze", so the reader
    // gets the finished diagram instead of a blank one.
    if (matchMedia('(prefers-reduced-motion: reduce)').matches) {
      document.querySelectorAll('svg[data-pf-anim]').forEach((s) => {
        if (typeof s.setCurrentTime === 'function') { s.setCurrentTime(8); s.pauseAnimations(); }
      });
    }
  } catch (e) { console.warn('[postforge] block 3 failed', e); }
}
